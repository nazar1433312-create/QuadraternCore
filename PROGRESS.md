# testnet-1 implementation — progress snapshot

## Готово, закоммичено и проверено сборкой + тестами

1. **`src/primitives/block.cpp`** — `GetPoWHash()` переключён со Scrypt на SHA-256d (`SerializeHash`, тот же алгоритм, что и `GetHash()`). Компилируется чисто.
2. **`src/chainparams.cpp`** — `CMainParams` (используется как сеть Quadratern testnet-1, не как понятие "mainnet" Bitcoin/Litecoin) переписан целиком:
   - Все Litecoin-исторические высоты софтфорков (BIP16/34/65/66/CSV/Segwit) обнулены — это новая цепь, не реплей истории Litecoin.
   - Taproot и MWEB отключены через `NEVER_ACTIVE`/`NO_TIMEOUT` (не удалены из кода — отключены штатным механизмом деплоймента, безопаснее, чем вырезать код, как это было сделано неаккуратно в MyCoin).
   - `pchMessageStart` = `QTRN` (0x51 0x54 0x52 0x4e), порт 29333, `bech32_hrp` = `qtrn`, свои base58-префиксы.
   - `powLimit` занижен (compact `0x1f00ffff`) — простая цель для старта с низким хешрейтом на testnet-1.
   - Genesis-блок намайнен заново под новый (SHA-256d) PoW: `nTime=1798348800`, `nNonce=13755`, hash и merkle root прописаны и проверяются через `assert()` в коде.
   - `nPowTargetSpacing = 60` (1 минута, по спеке).
   - DNS-сиды, фикс-сиды и чекпоинты Litecoin убраны (пустые/только genesis).
   - Компилируется чисто (`libbitcoin_common_a-chainparams.o`).
3. **`src/validation.cpp`** — `GetBlockSubsidy()` переписан на точную таблицу эмиссии QTRN (все 8 циклов + tail-эмиссия 1 QTRN), пороги посчитаны и задокументированы в блоках. Компилируется чисто.
4. **`src/qtrn/stake_selection.h` / `.cpp`** — `SelectStakeValidator()` (взвешенный детерминированный выбор, канонический порядок кандидатов по `id` внутри функции — результат не зависит от порядка на входе) и `DeriveFallbackSeed()` (доменно-разделённый хэш через `CHashWriter`, не сырое сложение). Подключено к `Makefile.am`/`Makefile.test.include`.
5. **`src/test/qtrn_stake_selection_tests.cpp`** — 7 юнит-тестов (пустой список, все веса ≤0, единственный кандидат, независимость от порядка входа, нулевой вес никогда не выигрывает, статистическая чувствительность к весу, детерминированность/отсутствие коллизий fallback-seed).
6. **`src/qtrn/stake_commitment.h` / `.cpp`** — формат передачи PoS-подписи в блоке выбран и реализован: одна выходная транзакция coinbase с `OP_RETURN <магия "QSTS"> <attempt: 1 байт> <compressed pubkey: 33 байта> <DER-подпись>`. `BuildStakeCommitmentScript()`/`FindStakeCommitment()` — сборка/парсинг; `VerifyStakeCommitment()` — проверка подписи + соответствия pubkey ожидаемому `id` кандидата.
7. **`ComputeStakeSigningHash(const CBlock&)`** — решает циклическую зависимость (подпись живёт в coinbase → входит в merkle root → входит в хэш блока, который она же подписывает): берёт coinbase, вычищает из него commitment-вывод (если он там уже есть), пересчитывает merkle root с "очищенным" coinbase, хэширует получившийся заголовок. Одна и та же функция обслуживает и майнера (коммита ещё нет — вычищать нечего) и валидатора (коммит есть — вычищается), давая один и тот же хэш по обе стороны. Реализовано по образцу `GenerateCoinbaseCommitment`/`BlockWitnessMerkleRoot` (SegWit-прецедент, уже был в этом дереве).
8. **`src/test/qtrn_stake_commitment_tests.cpp`** — 11 юнит-тестов: 8 на сборку/парсинг/верификацию + 3 на `ComputeStakeSigningHash`, включая полный сквозной сценарий (посчитать хэш без коммита → подписать → добавить коммит в coinbase → пересчитать хэш уже с коммитом → убедиться, что совпал с исходным → верификация проходит).
9. **`src/qtrn/stake_pool.h` / `.cpp`** — чистая логика агрегации виртуального стейкинг-пула (спека §6.3): `AssembleStakeCandidates()` группирует плоский список `{ownerId, amount}` по владельцу (суммируя все его вклады — реалистично: у одного адреса обычно несколько UTXO), затем — баланс ≥100 QTRN всегда становится отдельным кандидатом, баланс <100 QTRN сворачивается в пул. **Механизм привязки к пулу решён и реализован:** `DeterministicPoolId(ownerId) = Hash(ownerId) mod 64` — без регистрации в цепи (решение принято осознанно: пул математически не влияет на ожидаемую награду держателя, только на дисперсию — переигрывать нечего, значит нет смысла тратить транзакцию на выбор).
10. **`src/test/qtrn_stake_pool_tests.cpp`** — 10 юнит-тестов: пустой вход, соло-баланс, сумма нескольких UTXO одного владельца, сброс нулевого/отрицательного баланса, схлопывание в детерминированный пул, разные владельцы в одном пуле суммируются, агрегированная сумма пересекает порог (хотя каждый вклад по отдельности — нет), стабильность/диапазон `DeterministicPoolId`, соло игнорирует пул, смешанный реалистичный сценарий.

11. **`src/consensus/params.h`** — добавлено поле `genesisStakers` (`std::vector<std::pair<CPubKey, CAmount>>`) с обоснованием прямо в комментарии (см. решение о масштабе ниже).
12. **`src/chainparams.cpp` (`CMainParams`)** — зафиксирован фиксированный список из 3 genesis-стейкеров: один соло (500 QTRN), два в пул (30+40 QTRN) — специально подобраны, чтобы на реальных данных цепи проверялись оба пути (соло и пул). Одноразовые тестовые secp256k1-ключи, приватные ключи задокументированы в `contrib/testnet1-genesis-stakers.md` с явным предупреждением "не переиспользовать".
13. **`src/qtrn/genesis_stakers.h` / `.cpp`** — `GenesisStakersToBalances()`: мост между `Consensus::Params::genesisStakers` и `stake_pool`, использует тот же `StakeValidatorIdFromPubKey`, что и `stake_commitment` — то есть подпись genesis-стейкера верифицируется ровно против того `id`, который ему присвоил `stake_pool`.
14. **`src/test/qtrn_genesis_stakers_tests.cpp`** — 2 теста, из них один **полный сквозной пайплайн на реальных данных**: `Params().GetConsensus().genesisStakers` → `AssembleStakeCandidates` → `SelectStakeValidator` → подпись настоящим приватным ключом → `BuildStakeCommitmentScript` → `ComputeStakeSigningHash` на честном `CBlock` → `VerifyStakeCommitment` — успех. Это первое доказательство, что все четыре модуля (`stake_selection`+`stake_commitment`+`stake_pool`+`genesis_stakers`) реально стыкуются друг с другом, а не только проходят каждый свои изолированные тесты.

**Все 30 тестов (`stake_selection` 7 + `stake_commitment` 11 + `stake_pool` 10 + `genesis_stakers` 2) прошли: `Running 30 test cases... *** No errors detected`.**

Полный цикл проверен: `litecoind` собирается и линкуется, `test_litecoin` собирается и весь наш набор тестов зелёный.

## Найден и исправлен реальный баг (не мой новый код, но задел его же тестами)

`CTestNetParams`/`CRegTestParams` в `chainparams.cpp` использовали тот же общий хелпер `CreateGenesisBlock(nTime, nNonce, ...)`, что и `CMainParams` — когда туда прописали Quadratern-специфичные `pszTimestamp`/`genesisOutputScript`, testnet/regtest генезисы молча пересчитались на другие значения, а их собственные `assert(hashGenesisBlock == ...)` остались со старыми стоковыми litecoin-хэшами. Раньше это не всплывало, потому что ни один тест до `qtrn_stake_commitment_tests` не использовал фикстуру `BasicTestingSetup` (она при инициализации конструирует все три набора chainparams). Итог отказа — `SIGABRT` на `assert()` в конструкторе `CTestNetParams`, и каскадом — та же ошибка на всех следующих тестах (процесс не размотался чисто после `abort()`, `ArgsManager` остался в грязном состоянии). **Исправлено:** `CMainParams` теперь вызывает 7-аргументный `CreateGenesisBlock()` напрямую со своими значениями, не трогая общий хелпер — testnet/regtest вернулись к стоковым litecoin-генезисам, их asserts снова верны.

## Начато, не закончено

Правило консенсуса "блок без подписи стейкера недействителен" **всё ещё не подключено** нигде в `validation.cpp`/`net_processing.cpp`. Источник данных для кандидатов теперь есть (фиксированный genesis-список, п.11-14 выше) и весь пайплайн доказан сквозным тестом на реальных ключах — остаётся только:
- Сама точка подключения в `validation.cpp`/`net_processing.cpp` (плюс сторона майнинга — `miner.cpp` должен научиться дожидаться подписи и добавлять commitment-вывод перед публикацией блока).
- Полный адресный индекс ("любой баланс может стейкать") — осознанно отложен на testnet-2, см. ниже.

### Исследование адресного индекса и решение о масштабе testnet-1

Проверял, можно ли обойтись без полноценного индекса — просто сканировать текущий UTXO-набор раз в блок. **Тупик:** `CCoinsViewCache::Cursor()` (актуальное состояние цепи в памяти) в этом дереве явно кидает `std::logic_error("cursor iteration not supported")` — итерация по кэшу не поддерживается вообще, а курсор по дисковому слою (`CCoinsViewDB`) отражает устаревший снимок (до последнего flush, который бывает сильно позади текущего типа). Значит полноценный инкрементальный индекс (как `TxIndex`) — не опция для оптимизации, а единственный рабочий путь для "любой баланс может стейкать".

Хорошая новость: нашёл, что `UndoReadFromDisk(CBlockUndo&, const CBlockIndex*)` уже даёт для каждого блока `vtxundo[i].vprevout[j]` — полный `Coin` (адрес+сумма), потраченный каждым входом. Строить свою теневую UTXO-таблицу для дебета не нужно — можно переиспользовать уже существующий undo-механизм Litecoin/Bitcoin. Но `BaseIndex` (образец `TxIndex`) — это всё равно полноценная подсистема: фоновый поток синхронизации, прерывания, resume по locator, двухфазный `Commit()`, подключение в `init.cpp`. По объёму — больше, чем `stake_selection`+`stake_commitment`+`stake_pool` вместе.

**Решение (не согласовано явно, принято по ходу работы — можно пересмотреть):** для testnet-1 намеренно упрощаем масштаб. Веха testnet-1 в `ROADMAP.md` формулируется как "проверить сам факт работы гибридной схемы", не как продакшн-масштабируемость. Реальные PoS-сети (Ethereum, Cardano) тоже бутстрапятся с фиксированного набора genesis-валидаторов, а не с полного адресного индекса с первого дня — это стандартная, а не урезанная практика. **testnet-1 использует небольшой фиксированный список genesis-стейкеров** (см. следующий шаг), полный адресный индекс ("любой баланс может стейкать") — осознанно переносится на testnet-2.

## Уроки из процесса проверки (важно не повторить)

- **Не редактировать файлы репозитория, пока фоновая сборка идёт против того же смонтированного каталога.** Однажды это привело к тому, что Automake посреди `make` заметил, что `Makefile.am` новее сгенерированного `Makefile`, и запустил maintainer-mode пересборку конфигурации — результат стало невозможно проверить по логу.
- **Не редактировать (`sed -i` и подобное) лог-файл, в который ещё пишет живой фоновый процесс.** `sed -i` пересоздаёt файл (новый inode); процесс, открывший файл раньше через `>`, продолжает писать в старый (уже отвязанный от пути) inode — весь дальнейший вывод теряется, а через путь виден только "снимок" на момент правки.
- Итог обоих уроков: дождаться завершения фоновой сборки, ничего не трогая в `~/quadraterncore` и в её лог-файле, и только потом действовать.
- `arith_uint256` не имеет `operator%` (только `+`, `-`, `*`, `/`, сравнения) — модуль считался через `a - (a/b)*b`.
- Проверять только целевой тестовый бинарник (`make -C src test/test_litecoin`) и запускать только свой набор тестов (`--run_test=qtrn_stake_selection_tests`) — намного дешевле по времени/ресурсам, чем полный `make` + весь тестовый набор Litecoin, особенно на нагруженном сервере (в моменте load average доходил до 6.4 на 6 ядрах из-за параллельных задач на сервере).

## Следующие шаги при возобновлении

1. Подключить правило консенсуса "нет/неверный commitment стейкера => блок невалиден" в `validation.cpp`/`net_processing.cpp`, используя `stake_selection`+`stake_commitment`+`stake_pool`+`genesis_stakers` — весь пайплайн уже доказан сквозным тестом, остаётся только точка вызова в реальной проверке блока.
2. Сторона майнинга (`miner.cpp`): дождаться подписи выбранного валидатора (с 15-секундным таймаутом и фолбэком — см. спеку §6.2) и добавить commitment-вывод в coinbase перед публикацией блока.
3. (testnet-2, не блокирует testnet-1) Полноценный адресный индекс по образцу `BaseIndex`/`txindex.cpp`, используя `UndoReadFromDisk` для дебета по потраченным входам — заменит фиксированный `genesisStakers` на "любой баланс может стейкать".
4. Проверить сборкой + тестами после каждого шага, не совмещая правки с работающей фоновой сборкой.
5. Если фикстура `BasicTestingSetup` используется где-то ещё — держать в уме, что она инициализирует все три набора chainparams разом; при добавлении новой сети/параметров сверять genesis-related asserts во всех трёх классах, не только в том, что реально меняли.
